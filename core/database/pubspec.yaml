name: database
description: "Database module"
version: 1.0.0+1
publish_to: none

resolution: workspace

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  module:
    androidX: true
    androidPackage: com.flutterexample.core.database
    iosBundleIdentifier: com.flutterexample.core.database
