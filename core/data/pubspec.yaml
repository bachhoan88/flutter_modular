name: core_data
description: "Data module"

version: 1.0.0+1
publish_to: none

resolution: workspace

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  freezed_annotation: ^3.0.0
  json_annotation: ^4.8.1
  core_model:
    path: ../model
  core_common:
    path: ../common
  core_network:
    path: ../network
  equatable: ^2.0.5
  riverpod: ^2.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  build_runner: ^2.4.8
  freezed: '>=3.1.0 <3.2.0'
  json_serializable: ^6.7.1
  mockito: ^5.4.4

flutter:
  module:
    androidX: true
    androidPackage: com.flutterexample.core.data
    iosBundleIdentifier: com.flutterexample.core.data
